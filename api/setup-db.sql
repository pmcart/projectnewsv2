-- Create the database if it doesn't exist
CREATE DATABASE projectnews;

-- Connect to the database (you'll need to run this separately)
-- \c projectnews

-- Grant privileges (adjust username if needed)
GRANT ALL PRIVILEGES ON DATABASE projectnews TO postgres;
